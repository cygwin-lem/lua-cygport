--- origsrc/lua-5.1.5/Makefile	2012-02-10 18:50:23.000000000 +0900
+++ src/lua-5.1.5/Makefile
@@ -41,10 +41,13 @@
 PLATS= aix ansi bsd freebsd generic linux macosx mingw posix solaris
 
 # What to install.
-TO_BIN= lua luac
+LUA_T?= lua
+LUAC_T?= luac
+
+TO_BIN= $(LUA_T)$(EXEC_SUFFIX) $(LUAC_T)$(EXEC_SUFFIX)
 TO_INC= lua.h luaconf.h lualib.h lauxlib.h ../etc/lua.hpp
 TO_LIB= liblua.a
-TO_MAN= lua.1 luac.1
+TO_MAN= $(LUA_T).1 $(LUAC_T).1
 
 # Lua version and release.
 V= 5.1
@@ -56,14 +59,16 @@
 	cd src && $(MAKE) $@
 
 test:	dummy
-	src/lua test/hello.lua
+	src/$(LUA_T) test/hello.lua
 
 install: dummy
 	cd src && $(MKDIR) $(INSTALL_BIN) $(INSTALL_INC) $(INSTALL_LIB) $(INSTALL_MAN) $(INSTALL_LMOD) $(INSTALL_CMOD)
 	cd src && $(INSTALL_EXEC) $(TO_BIN) $(INSTALL_BIN)
 	cd src && $(INSTALL_DATA) $(TO_INC) $(INSTALL_INC)
 	cd src && $(INSTALL_DATA) $(TO_LIB) $(INSTALL_LIB)
-	cd doc && $(INSTALL_DATA) $(TO_MAN) $(INSTALL_MAN)
+	sed -e 's|^lua |'$(LUA_T)' |;' -e 's|^\.B lua$$|.B '$(LUA_T)'|;' -e 's|^\.BR luac |.BR '$(LUAC_T)' |;' < doc/lua.1 > src/$(LUA_T).1
+	sed -e 's|^luac |'$(LUAC_T)' |;' -e 's|^\.B luac$$|.B '$(LUAC_T)'|;' -e 's|^\.BR lua |.BR '$(LUA_T)' |;' < doc/luac.1 > src/$(LUAC_T).1
+	cd src && $(INSTALL_DATA) $(TO_MAN) $(INSTALL_MAN)
 
 ranlib:
 	cd src && cd $(INSTALL_LIB) && $(RANLIB) $(TO_LIB)
--- origsrc/lua-5.1.5/src/Makefile	2012-02-14 05:41:22.000000000 +0900
+++ src/lua-5.1.5/src/Makefile
@@ -36,7 +36,7 @@ LUAC_T=	luac
 LUAC_O=	luac.o print.o
 
 ALL_O= $(CORE_O) $(LIB_O) $(LUA_O) $(LUAC_O)
-ALL_T= $(LUA_A) $(LUA_T) $(LUAC_T)
+ALL_T= $(LUA_A) $(LUA_T)$(EXEC_SUFFIX) $(LUAC_T)$(EXEC_SUFFIX)
 ALL_A= $(LUA_A)
 
 default: $(PLAT)
@@ -51,10 +51,10 @@ $(LUA_A): $(CORE_O) $(LIB_O)
 	$(AR) $@ $(CORE_O) $(LIB_O)	# DLL needs all object files
 	$(RANLIB) $@
 
-$(LUA_T): $(LUA_O) $(LUA_A)
+$(LUA_T)$(EXEC_SUFFIX): $(LUA_O) $(LUA_A)
 	$(CC) -o $@ $(MYLDFLAGS) $(LUA_O) $(LUA_A) $(LIBS)
 
-$(LUAC_T): $(LUAC_O) $(LUA_A)
+$(LUAC_T)$(EXEC_SUFFIX): $(LUAC_O) $(LUA_A)
 	$(CC) -o $@ $(MYLDFLAGS) $(LUAC_O) $(LUA_A) $(LIBS)
 
 clean:
